package com.bank.portal.repository; import com.bank.portal.domain.CreditCard; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface CardRepository extends JpaRepository<CreditCard,Long>{Optional<CreditCard> findByCardNumber(String cardNumber); List<CreditCard> findByCustomerId(Long customerId); long countByStatus(com.bank.portal.domain.CardStatus status);}
