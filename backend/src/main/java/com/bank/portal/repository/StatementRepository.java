package com.bank.portal.repository; import com.bank.portal.domain.MonthlyStatement; import java.time.LocalDate; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface StatementRepository extends JpaRepository<MonthlyStatement,Long>{ Optional<MonthlyStatement> findByCardIdAndPeriodStart(Long cardId,LocalDate periodStart); List<MonthlyStatement> findByCustomerIdOrderByPeriodStartDesc(Long customerId); }
